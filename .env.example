# Render supplies PORT automatically. Keep this value for local API runs.
PORT=8080

# Optional SMTP preparation for future email delivery.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CONTACT_EMAIL=

# Optional structured server logging level.
LOG_LEVEL=info